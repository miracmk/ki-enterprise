services:
  prometheus:
    image: prom/prometheus:latest
    container_name: ki-enterprise-prometheus
    restart: unless-stopped

    ports:
      - "19090:9090"

    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

    networks:
      - ki-enterprise

networks:
  ki-enterprise:
    external: true
